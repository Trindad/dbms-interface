/**
* gcc *.c insert/semantic/semantic.c -o bf -g -ll -ly
 */
#include "shell.h"

int main(){
    
    example();//executa a criação de um exemplo, com banco, tabelas e inserções.
    shell();//executa interface

    return 0;
}